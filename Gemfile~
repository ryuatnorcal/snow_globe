source 'https://rubygems.org'

gem 'rails', '3.2.5'
# Amazing Styling foundation
gem "zurb-foundation", :group => :assets
# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'
gem 'execjs'
gem 'therubyracer', '0.11.0beta5'
gem 'libv8'
gem 'devise', '2.1.0.rc2' # Devise login system
gem 'geocoder' # Geographic encorder. using google map api
gem 'jquery-rails' # JQuery for rails project 
gem 'will_paginate', '~> 3.0' #endless paginate
gem "sqlite3", :groups => [:development, :test]
#gem "pg", :groups => [:production]

#gem 'prototype-rails', :git => 'git://github.com/rails/prototype-rails.git' #rjs
# Carrierwave application which handles user's uploadpicture files 
gem "rmagick"
gem "carrierwave"
# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end



# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
